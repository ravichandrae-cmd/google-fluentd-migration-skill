# Upstream Migrated Configuration (v1 Standard)
# Generated automatically using upstream-config-migrator SKILL.md

# WARNING [MIGRATION EDGE CASE]:
# The following legacy parameters could not be automatically mapped to upstream format:
# - Parameter: 'custom_mongodb_buffer_tuning'
# Action Required: Review upstream documentation for equivalent tuning options.

# Legacy MongoDB Logging Configuration
<source>
  @type tail
  path /var/log/mongodb/mongod.log
  pos_file /var/log/fluentd/mongodb.pos
  tag mongodb.server
  <parse>
    @type json
    # NOTE: auto_typecast directive stripped (unsupported in v1 JSON parser)
  </parse>
</source>

<filter mongodb.server>
  @type record_transformer
  <record>
    database "mongodb-prod"
    environment "production"
  </record>
</filter>

<match mongodb.server>
  @type google_cloud
  use_grpc true
  grpc_compression_algorithm gzip
    # NOTE: auto_typecast directive stripped (unsupported in v1 JSON parser)
  # [UNMAPPED LEGACY DIRECTIVE COMMENTED FOR V1 SAFETY] custom_mongodb_buffer_tuning true
</match>
