# Upstream Migrated Configuration (v1 Standard)
# Generated automatically using upstream-config-migrator SKILL.md

# WARNING [MIGRATION EDGE CASE]:
# The following legacy parameters could not be automatically mapped to upstream format:
# - Parameter: 'gitlab_custom_routing'
# Action Required: Review upstream documentation for equivalent tuning options.

# Legacy GitLab JSON Log Configuration
<source>
  @type tail
  path /var/log/gitlab/gitlab-rails/production_json.log
  pos_file /var/log/fluentd/gitlab_production.pos
  tag gitlab.production
  <parse>
    @type json
    time_format %Y-%m-%dT%H:%M:%S.%LZ
    time_key time
    # NOTE: auto_typecast directive stripped (unsupported in v1 JSON parser)
  </parse>
</source>

<filter gitlab.production>
  @type record_transformer
  enable_ruby true
  <record>
    app "gitlab"
    raw_timestamp ${time.to_i}
  </record>
</filter>

<match gitlab.production>
  @type google_cloud
  use_grpc true
  grpc_compression_algorithm gzip
  <buffer>
    @type file
    path /var/log/google-fluentd/buffers/gitlab
    chunk_limit_size 512KB
    flush_interval 10s
    retry_max_times 5
    flush_thread_count 4
  </buffer>
  # NOTE: partial_success directive stripped (permanently enabled in v1)
  # [UNMAPPED LEGACY DIRECTIVE COMMENTED FOR V1 SAFETY] gitlab_custom_routing true
</match>
