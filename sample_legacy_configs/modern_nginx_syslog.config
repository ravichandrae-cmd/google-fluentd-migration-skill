# Upstream Migrated Configuration (v1 Standard)
# Generated automatically using upstream-config-migrator SKILL.md

# WARNING [MIGRATION EDGE CASE]:
# The following legacy parameters could not be automatically mapped to upstream format:
# - Parameter: 'custom_legacy_buffer_option'
# Action Required: Review upstream documentation for equivalent tuning options.

# Sample Legacy Fluentd / Ops Agent Configuration
<source>
  @type tail
  path /var/log/nginx/access.log
  pos_file /var/log/fluentd/nginx-access.log.pos
  tag nginx.access
  <parse>
    @type json
    # NOTE: auto_typecast directive stripped (unsupported in v1 JSON parser)
  </parse>
</source>

<source>
  @type syslog
  port 5140 # NOTE: Shifted to unprivileged port 5140
  bind 0.0.0.0
  tag syslog.system
</source>

<filter nginx.access>
  @type record_transformer
  <record>
    environment "production"
    cluster "us-central1-a"
  </record>
</filter>

<match **>
  @type google_cloud
  use_grpc true
  grpc_compression_algorithm gzip
    # NOTE: auto_typecast directive stripped (unsupported in v1 JSON parser)
  # [UNMAPPED LEGACY DIRECTIVE COMMENTED FOR V1 SAFETY] custom_legacy_buffer_option true
</match>
