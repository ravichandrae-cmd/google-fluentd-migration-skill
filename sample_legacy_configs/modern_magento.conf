# Upstream Migrated Configuration (v1 Standard)
# Generated automatically using upstream-config-migrator SKILL.md

# WARNING [MIGRATION EDGE CASE]:
# The following legacy parameters could not be automatically mapped to upstream format:
# - Parameter: 'magento_flush_legacy'
# Action Required: Review upstream documentation for equivalent tuning options.

# Legacy Magento System Log Configuration
<source>
  @type tail
  path /var/www/html/magento2/var/log/system.log
  pos_file /var/log/fluentd/magento_system.pos
  tag magento.system
  <parse>
    @type regexp
    expression /^\[(?<time>\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2})\] (?<logger>[a-z_]+)\.(?<level>[A-Z]+): (?<message>.*)$/
    time_format %Y-%m-%d %H:%M:%S
    # NOTE: auto_typecast directive stripped (unsupported in v1 JSON parser)
  </parse>
</source>

<filter magento.system>
  @type record_transformer
  enable_ruby true
  <record>
    raw_timestamp ${time.to_i}
    app_platform "magento2"
  </record>
</filter>

<match magento.system>
  @type google_cloud
  use_grpc true
  grpc_compression_algorithm gzip
  <buffer>
    @type file
    path /var/log/google-fluentd/buffers/magento
    chunk_limit_size 128KB
    flush_interval 10s
    retry_max_times 3
    flush_thread_count 2
  </buffer>
  # NOTE: partial_success directive stripped (permanently enabled in v1)
  # [UNMAPPED LEGACY DIRECTIVE COMMENTED FOR V1 SAFETY] magento_flush_legacy true
</match>
