# Upstream Migrated Configuration (v1 Standard)
# Generated automatically using upstream-config-migrator SKILL.md

# WARNING [MIGRATION EDGE CASE]:
# The following legacy parameters could not be automatically mapped to upstream format:
# - Parameter: 'salt_master_ip'
# Action Required: Review upstream documentation for equivalent tuning options.

# Legacy Salt Minion Log Configuration
<source>
  @type tail
  path /var/log/salt/minion
  pos_file /var/log/fluentd/salt_minion.pos
  tag salt.minion
  <parse>
    @type multiline
    format_firstline /^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2},\d{3}/
    format1 /^(?<time>\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2},\d{3}) \[(?<logger>[^\]]+)\]\[(?<level>[A-Z]+)\]\[(?<pid>\d+)\] (?<message>.*)$/
    time_format %Y-%m-%d %H:%M:%S,%f
  </parse>
</source>

<filter salt.minion>
  @type record_transformer
  enable_ruby true
  <record>
    app "saltstack"
    legacy_timestamp ${time.to_i}
  </record>
</filter>

<match salt.minion>
  @type google_cloud
  use_grpc true
  grpc_compression_algorithm gzip
  <buffer>
    @type file
    path /var/log/google-fluentd/buffers/salt
    chunk_limit_size 2M
    flush_interval 5s
  </buffer>
  # [UNMAPPED LEGACY DIRECTIVE COMMENTED FOR V1 SAFETY] salt_master_ip 10.0.0.1
</match>
