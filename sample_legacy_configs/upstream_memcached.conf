# WARNING [MIGRATION EDGE CASE]:
# The following legacy parameters could not be automatically mapped to upstream format:
# - Parameter: 'custom_memcached_buffer_opt' in <match memcached.cache>
# Action Required: Review upstream documentation for equivalent buffer tuning options.

# Upstream Migrated Memcached Configuration

# Memcached Cache Log Source
<source>
  @type tail
  path /var/log/memcached.log
  pos_file /var/log/fluentd/memcached.pos
  tag memcached.cache
  <parse>
    @type syslog
    # NOTE: 'auto_typecast' directive stripped (unsupported in upstream parser)
  </parse>
</source>

# Dynamic Customer Metadata Labels
<filter memcached.cache>
  @type record_transformer
  <record>
    service "memcached-cache"
    environment "production"
  </record>
</filter>

# Google Cloud Output Plugin
<match memcached.cache>
  @type google_cloud
  # NOTE: 'auto_typecast' directive stripped (unsupported in upstream parser)
  use_grpc true
</match>
