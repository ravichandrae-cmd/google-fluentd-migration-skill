@include config.d/*.conf

<filter **>
  @type analyze_config
</filter>

<filter **>
  @type add_insert_ids
</filter>

<match **>
  @type google_cloud
  buffer_path /var/lib/google-fluentd/buffers
</match>
